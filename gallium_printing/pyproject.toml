[project]
name = "gallium_deposition"
version = "0.1.0"

[project.scripts]
gallium_printing = "gallium_printing.main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"